# PW Test Workspace

This repository is the generated distribution target for the AIProd Playwright test workspace.

The canonical source is the `aiprod` framework repository. Contents of this repository are refreshed by GitHub Actions and consumed with `npx`:

```powershell
npx --yes --package github:galechrisc654-lgtm/pw-test#v0.1.0 pw-test-workspace update "D:\your-test-workspace"
```
